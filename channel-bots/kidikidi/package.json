{
  "name": "kidikidi-order-bot",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "키디키디(E·LAND 파트너오피스) 주문 수집 봇 — 로그인(TOTP 자체생성) → 내부 주문 API → Supabase",
  "scripts": {
    "sync": "node sync.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "otplib": "^12.0.1",
    "playwright": "^1.47.0"
  }
}
