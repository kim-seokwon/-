# (주)이일칠구 어드민 — 프로젝트 현황

> 최종 정리: 2026-07-29 · 담당 자동화: 매출수집 / 송장발번 / 재고 / 견적·세금계산서
> 스택: Vite + 바닐라 JS SPA(`main.js`) + Supabase(Edge Functions) + GitHub Actions 봇
> 배포: `git push origin main` → Vercel **bhas-blue.vercel.app**
> Supabase project: `czaykmmwzlcisozmbxpl`

---

## ✅ 완료 · 가동 중 (지금 실제로 돌아감)

### 1. 채널 매출 자동수집 (GitHub Actions 매시간 크론)
| 채널 | 방식 | 상태 |
|---|---|---|
| 카페24 3몰 (토비=thehime1 / 하이헤이호=myho1129 / 로하이=thehimeseller) | `cafe24-sync` Edge Function | ✅ 전체 이력 수집. **N50(구매확정) 버그 수정**으로 누락분 복구 |
| 키디키디 (E·LAND 파트너오피스) | 봇(Playwright + TOTP 자체생성), 주문 API 역추적 | ✅ 가동. 737건 수집 |
| 29CM (무신사 통합파트너) | 봇, **이메일 OTP**(Gmail API 자동읽기) | ✅ 로그인·수집 작동. 현재 실주문 0건이라 0건(정상) |

- 세션 중 확인 총 주문 ≈ **3,072건** (하이헤이호1653·키디키디737·로하이365·토비315)

### 2. 웹앱 (bhas-blue.vercel.app)
- **HOME 대시보드**: BI형(주문/생산/매출 KPI, 채널별 브랜드색 바, 도넛 %, 최근주문 스크롤)
- **매출 페이지**: 년+월 선택(26년 1월부터 조회), 단월=일별/전체=월별 차트, 반품·교환 급증+반복고객 분석, 브랜드운영 인사이트(채널 순이익·재구매율·옵션분포)
- **연동 화면**: 브랜드×채널 매트릭스 (카페24 연동/인증, 택배사 선택)
- 타이틀 "주식회사 이일칠구"

### 3. 카페24 재고 차감
- 카페24 판매 시 `inventory_ledger` 자동 차감 (멱등: ref = mall:order:variant)

### 4. 우체국 송장 발번 (courier-issue) — 셋업·API 검증 완료
- 우체국 계약소포 OpenAPI 직접, SEED128 암호화(`courier-issue` Edge Function)
- EPOST 시크릿 5개 Supabase에 설정, **테스트 발번(testYn=Y) 필드검증 통과 = 암호화·계약·공급지 유효**
- OMS 배송탭에 **"우체국 발번+송장등록"** 원클릭 버튼(발번→운송장번호→카페24/채널 송장 입력)

---

## ⚠️ 미비 · 남은 작업

### A. 카페24 "완벽 라이브"까지 (오픈 전 필수)
- [ ] **dry_run 해제** — 현재 `channel_sync_state.dry_run=true`. 실제 카페24로 재고·송장 전송이 꺼져 있고 어드민만 갱신 중
- [ ] dry_run 켜기 전 **재고 숫자가 카페24와 맞는지 확인** (안 맞으면 카페24 재고가 덮여써서 위험)
- [ ] **통제된 테스트 주문 1건**으로 발번→카페24 송장입력→재고 흐름 실제 확인 (실고객 주문 X)

### B. 키디키디·29CM 송장 역등록 (오픈 직전 마무리)
- [ ] 각 채널 "송장등록/발송처리" API 리버스엔지니어링 필요
  - **블라인드로는 불가** 확인됨(진단 3회): E·LAND 배송관리가 중첩 JS SPA라 헤드리스로 API 캡처 안 됨
  - → **오픈 직전 실주문(또는 통제된 테스트주문) 1건**이 있을 때 인터랙티브로 캡처해야 안전. 현재 배송준비 0건이라 대기
- [ ] 현재 우체국 발번 시 키디키디·29CM은 어드민 DB(`channel_orders`)에만 송장 기록됨 (그 채널엔 미반영)

### C. 키디키디·29CM 재고 차감
- [ ] 채널 상품 ↔ 내부 SKU **매핑(`inventory_listings`) 선행 필요** (현재 카페24만 매핑됨)
- [ ] 매핑 후 봇/공유함수에 차감 로직 추가

### D. 기타 자동화 (자격증명/가입 대기 = 사용자 액션)
- [ ] **스마트스토어 봇** — 네이버 커머스API 앱 등록(apicenter.commerce.naver.com) → 키 발급
- [ ] **세금계산서(팝빌)** — 팝빌 연동회원 가입(사업자번호) → LinkID/SecretKey. 견적 데이터 기반 발행 UI도 필요
- [ ] **보안**: 초반 노출된 cafe24 Client Secret **재발급**, GitHub repo **Private 전환**

---

## 🔑 핵심 정보 (자격증명은 값 대신 "위치"만 — repo에 비밀값 금지)

### 배포·인프라
- 코드 push → Vercel 자동배포(bhas-blue.vercel.app). ⚠️ iCloud 동기화 폴더라 빌드 함정 주의
- Supabase Edge Functions: cafe24-oauth / cafe24-sync / cafe24-shipping / courier-issue / taxinvoice-issue / kakao-quick (전부 ACTIVE)
- 비밀값 위치: **Supabase secrets**(Edge Function용) / **GitHub Secrets**(봇용). repo엔 넣지 말 것

### 채널별 특이사항 (함정 방지)
- **카페24**: 하이헤이호·토비는 이일칠구 계정 앱 공유(볼트 `CAFE24_SECRET_MYHO1129`), 로하이는 별도계정 앱(client_id `H0YjEAvpNhQjNSA8m2KC4A`, secret 볼트 `CAFE24_SECRET_THEHIMESELLER`). scope 불일치 시 ERR_TOO_MANY_REDIRECTS
- **키디키디**: E·LAND `po.elandbo.co.kr`, id `hiheyho2`, 2FA=**TOTP**(GitHub `ELAND_TOTP_SECRET`). 봇 자동로그인은 headless라 인터랙티브 조작은 못 함
- **29CM**: 무신사 통합 `partner-sso.one.musinsa.com`, id `hiheyho`, 2FA=**이메일 OTP**(TOTP 아님!). OTP 메일은 `mail@musinsa.com`→**thehime.official@gmail.com**. Gmail API(Google Cloud 프로젝트 `bhas-29cm-bot`, 테스트유저 thehime.official)로 자동 읽음. GitHub 시크릿 `CM29_ID/CM29_PW/GMAIL_CLIENT_ID/SECRET/REFRESH_TOKEN`

### 우체국 송장 (courier-issue)
- 인터넷우체국 계약고객시스템 `biz.epost.go.kr`, id/pw **hime24/hime24**, 상담 **1588-1300**(카톡 1:1도 가능)
- Supabase secrets(값은 거기 있음): `EPOST_REGKEY`(30자리 인증키), `EPOST_SECKEY`(보안키=SEED128키), `EPOST_CUSTNO`(고객번호), `EPOST_APPRNO`(승인번호=**GetApprNo API값** `1004281827`, 상담사가 준 40004-82920 아님), `EPOST_OFFICESER`=`01`
- ⚠️ **(주)맥가이버팩토리 = 택배대리점** (macfac.kr, 서울 마포, 윤성환, 02-6959-9112). officeSer=01 = 대리점 집화점 = 정상 출고지 (낯선 이름이라고 의심 금지)
- ⚠️ 보안키는 생성 후 30일 내 접수 없으면 만료 → 재생성 필요
- courier-issue 액션: `cust-no`/`appr-no`/`get-office`/`setup-office`/`issue`(발번, body.test=true면 testYn=Y 실발송없음)

### 매출 브랜드 (4개)
- 토비·하이헤이호·로하이스튜디오 = 몰 주문 / 브하스 = 컨설팅 견적·세금계산서

---

## 📋 오픈 전 체크리스트 (권장 순서)
1. 카페24 재고 정합성 확인 → **dry_run 해제** → 통제된 테스트주문 1건으로 발번·송장·재고 검증
2. 오픈 직후 첫 실주문(또는 테스트주문)으로 **키디키디·29CM 송장 역등록 API 캡처 → 코드 완성**
3. 키디키디·29CM 상품 ↔ 내부 SKU 매핑 → 재고 차감 켜기
4. 보안 마무리: cafe24 Client Secret 재발급 + GitHub repo Private
5. (여유) 스마트스토어 연동, 세금계산서 팝빌

> **원칙: 오픈 전 코드 다 붙이고 → 더미/테스트로 안전 검증 → 오픈. 실고객 주문으로 테스트 금지(송장 꼬임 방지).**
