# (주)이일칠구 어드민 — 프로젝트 현황

> 최종 정리: **2026-07-30** · 스택: Vite + 바닐라 JS SPA(`main.js`) + Supabase(Edge Functions/pg_cron) + GitHub Actions 봇
> 배포: `git push origin main` → Vercel **bhas-blue.vercel.app** (⚠️ iCloud 폴더 빌드주의)
> Supabase project: `czaykmmwzlcisozmbxpl`

---

## 🔑 로그인 / 계정 (재부팅 후 제일 먼저 볼 것)
- **마스터 로그인**: 아이디 칸에 **`ksw5363@gmail.com` (풀 이메일 전체)** + 비번. 성함="김석원"으로 표시됨.
  - ⚠️ `ksw5363`만 치면 앱이 `@bhas.com`을 붙여서 **로그인 실패**함 → 반드시 풀 이메일.
- **auth 계정은 마스터(ksw5363) 1개뿐.** 직원 계정은 앱 **계정 관리 → 새 계정 추가**(그건 `아이디@bhas.com` 규칙이라 아이디만 쳐도 됨).
  - 참고: `companies` 테이블엔 `bhas1`·`test` 행이 남아 있지만 auth 사용자가 없어 로그인 불가(무해).

---

## ✅ 2026-07-30 세션 완료

### 데이터 정합 (⭐ 가장 큰 건)
- **하이헤이호 주문 대량 누락 발견·복구** — DB 1,763건 → **10,233건**(신규 8,089건 저장).
  2025-06~2026-01은 **한 건도 없었고**, 있는 달도 절반 수준이었음(2025-03: DB 300 vs 실제 805).
  → 그동안 매출이 대규모 과소집계 상태였음. 카페24 `orders/count` 표본 7개월 **전부 정확히 일치** 확인.
- **취소 주문이 애초에 저장되지 않는 버그 수정** — 수집기가 `order_status=N00~N50`만 요청해서
  수집 전에 취소된 주문(`C48` 등)은 **영구 누락**됐음 → 상태 필터 제거(전체 상태 수집).
  대신 **취소(C)·반품(R) 아이템은 재고 차감에서 제외**(과거엔 취소건도 차감돼 재고가 음수로 갔음).
- **상태변경 재동기화(restatus) 신설** — 증분 수집은 최근 2일만 봐서 오래된 주문이 뒤늦게
  취소/환불/배송완료돼도 반영이 안 됐음. 이제 매 수집마다 최근 60일을 상태 필터 없이 재조회해
  **기존 주문의 `raw`/`channel_status`만 갱신**(신규삽입·재고차감 없음 → 안전).
  실측: 배송중(M/N30) → 배송완료(T/N40) 전이가 실제로 잡힘.
- **로하이·토비도 백필 완료** — 카페24 실제 건수와 월별 일치(±1~2는 KST/UTC 경계 차이).
- 총 주문 **12,187건** / 아이템 24,199건.

### 성능 (백필로 데이터가 6배 늘어 필수였던 작업)
- **경량 뷰 `channel_orders_slim`(019)** — `raw`(주문당 ~13KB 카페24 원본)를 전량 내려받아
  매출탭이 멈추는 문제. 집계가 실제 쓰는 필드만 같은 모양으로 재구성.
  → 전량 로드 **180MB → 6MB(23.5배↓)**, 12,187건 13페이지 ~16초. `_loadSalesOrders()`가 이 뷰를 씀.
- **`idx_orders_order_date` 추가(020)** — 기존 인덱스는 `(status, order_date)`뿐이라 전체기간 정렬에 안 쓰였음.

### 보안 (⭐ 심각한 구멍 막음)
- **`courier-issue`·`taxinvoice-issue`·`cafe24-shipping`·`kakao-quick`이 인증 없이 열려 있었음.**
  `--no-verify-jwt` + CORS `*` + **리포가 public** → 누구나 실제 **우체국 송장 발번(요금 발생)**,
  **세금계산서 발행(법적 문서)**, 고객 배송중 알림을 호출할 수 있었음.
- **`_shared/auth.ts` `requireUser()` 가드 추가** — 실제 로그인 세션(auth.users)만 통과.
  ※ 단순 JWT 검증은 무의미: **anon 공개키 자체가 유효한 서명 JWT**이고 `main.js`에 들어 있음.
  → 검증 완료: 익명 401 / anon 공개키 401 / 앱(`functions.invoke`, 사용자 JWT 자동첨부) 정상.
- `cafe24-sync`는 pg_cron이 공개키로 부르므로 **개방 유지**(읽기·동기화 전용, 멱등).
- **깃 히스토리 전체 스캔 결과 시크릿 커밋 이력 없음**(전 커밋 대상 JWT·client_secret 패턴 스캔). 문서 참조만 존재.

### 권한
- **마이그레이션 018 적용** — `companies.menu_access TEXT[]`, `brand_access UUID[]` 생성 확인.
  이제 계정별 메뉴·브랜드 권한 체크박스가 실제로 저장됨(그전엔 컬럼이 없어 저장 무시됨).

### 재고 안전장치
- **음수 재고 push 차단** — 매핑된 유일 상품이 `on_hand = -6`(기초재고 미입력 + 과거 취소건 오차감)이라
  dry_run을 끄면 실제 스토어 재고를 음수로 밀어버릴 상태였음. 이제 음수면 건너뛰고 `warn` 로그.

---

## ⚠️ 남은 작업 (오픈 전 우선순위)

### A. 리포 Private 전환 (사용자 작업, 5분)
`github.com/kim-seokwon/-` 가 **public**. 함수 가드는 넣었지만 구조·엔드포인트가 다 공개됨.
→ Settings → General → Danger Zone → Change visibility → Private.
(`gh` CLI가 이 맥에 없어서 대신 못 해줌. 웹에서 클릭 필요)

### B. 재고 연동 (dry_run 해제의 실제 선행조건)
- `dry_run`은 **주문 저장과 무관**(재고 push만 억제) — 로하이 주문은 정상 수집되고 있었음.
- 진짜 블로커: **채널상품↔내부SKU 매핑이 전 몰 합쳐 1건**뿐 + 그 1건도 **기초재고 미입력(on_hand -6)**.
- [ ] 매핑 등록 → [ ] 기초재고 입력 → [ ] 그 다음 dry_run 해제 → 통제된 테스트 1건 검증

### C. 키디키디 주소·연락처 (737건 전부 null)
- 목록 API에 수령인/주소/연락처가 없음 → E·LAND **상세조회 API 역추적 필요**.
- 자격증명(`ELAND_*`)이 **GitHub Secrets에만** 있어 로컬에서 못 돌림
  → `kidikidi-diag.yml`(workflow_dispatch)을 **Actions 탭에서 수동 실행**해야 엔드포인트 캡처 가능.
- 주소 없으면 **우체국 발번 불가**. 카페24 3몰은 주소·연락처 완비 → 발번 OK.

### D. 기타
- [ ] 키디키디·29CM 송장 역등록 API (오픈 직전 실주문으로 캡처)
- [ ] 스마트스토어 봇 (네이버 커머스API 키)
- [ ] 세금계산서 팝빌 (사업자 가입 → LinkID/SecretKey) — 시크릿 미설정 상태
- [ ] 정상 주문 중 `pay_amount` null 35건(T+N 14건, F+N 21건) 원인 확인
      ※ null 835건 중 815건은 취소건(취소 후 결제액 0)이라 정상

---

## 📌 핵심 정보 (함정 방지)

### 주문 상태 데이터 모델
- **내부 `status` 컬럼은 무의미**(수집 시 전량 'new'). 미출고 판별에 쓰지 말 것
- **카페24**: raw에 `order_status` 없음! → `raw.shipping_status`(F배송전/M배송중/T완료)
  + `raw.items[].order_status`(N40완료·N50구매확정·N30배송중·**C**취소·**R**반품·**E**교환)
  ※ 아이템 코드는 `status`가 아니라 **`order_status` 키**에 담겨 옴(`_orderState`는 둘 다 봄)
- **키디키디**: `channel_status` 숫자코드(1정상·2취소·3반품·4교환)

### 카페24 API 함정
- 주문조회 기간은 **"종료일이 시작일로부터 3개월 내"** 강제 → 89일 청크는 2월 구간에서 **422**.
  현재 청크 **80일**로 고정.
- **Edge Function 1회 호출로 전체기간 백필 불가**(`WORKER_RESOURCE_LIMIT`).
  → 기간을 20일씩 쪼개 순차 호출(백필 드라이버 방식). ~500건/호출이 안전선.
- `orders/count` 엔드포인트로 실제 건수 대조 가능 → **누락 점검은 이걸로**.

### 인프라 함정
- **Supabase 대시보드 = 크롬 자동번역 켜지면 튕김**(React insertBefore). supabase.com 번역 끄기
- SQL은 Management API로: `POST https://api.supabase.com/v1/projects/czaykmmwzlcisozmbxpl/database/query`
  Bearer = **맥 키체인** `security find-generic-password -s "Supabase CLI" -w` (supabase CLI 로그인 토큰)
  ⚠️ python urllib로 부르면 **Cloudflare 1010 차단** → `curl`로 호출할 것
- Edge Functions 배포: `supabase functions deploy <fn> --no-verify-jwt`
  (verify_jwt를 켜도 anon 키가 통과하므로 보호는 함수 내부 `requireUser()`로 함)
- 비밀값은 Supabase secrets / GitHub Secrets에만. repo 금지

### 매출 브랜드 (판매 3 + 생산 2)
- 판매: **하이헤이호**(자사몰 myho1129 + 키디키디 + 29CM) · **로하이스튜디오**(thehimeseller) · **토비**(thehime1)
- 생산만: 히얼유아·머문 (제품 있음, 판매채널 없음)

---

## 📋 재부팅 후 확인 체크리스트
1. bhas-blue 접속 → `ksw5363@gmail.com`(풀 이메일) + 비번 로그인
2. **⌘+Shift+R 하드 리프레시**(캐시 때문에 옛버전 뜰 수 있음)
3. 매출탭: **2025-01~2026-07 전 구간** 나오는지 (예: 2026-02 ≈ 9,557만 / 2025-04 ≈ 7,151만)
4. 홈 대시보드: 배송전/배송중/교환/환불 4박스 + "N분 전 동기화"(5분 이내 유지)
5. 매출탭 로딩이 오래 걸리면(12,187건 ~16초) 정상 — 6MB 전량 로드 중

> **원칙: 오픈 전 코드 다 붙이고 → 더미/테스트로 안전 검증 → 오픈. 실고객 주문으로 테스트 금지.**
